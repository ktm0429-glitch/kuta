rules_version = '2';

// すべての読み書きは Cloud Functions (Admin SDK) 経由のみに限定する。
// スタッフのポイントや完了記録をクライアントから直接改ざんできないようにするため、
// クライアントからの直接アクセスはすべて拒否する。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
