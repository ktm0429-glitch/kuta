# kuta

保存用(既存のMQL5関連ファイルはそのまま保管しています)

## 接客力向上トレーニングアプリ

アルバイトスタッフが勤務時間外に取り組む、遊技延長につながる会話力・接客力向上のための
研修アプリです。挑戦するたびに、全問題の中から**ランダムに5問**が出題され、各問題に
スタッフが**カメラ・マイクに向かって実際に声に出して回答**します。発話内容・表情・声の
トーンをその場で解析し、5問すべてが合格基準を満たすと1ptが付与されます
(1問でも基準未達だとその回はポイントなし)。
**ポイントは1日1人1ptが上限**で、同じ日に何回挑戦しても2pt以上には
なりません(翌日になるとまた1pt獲得できます)。全店舗のスタッフを「店舗 + フルネーム」で
登録・管理し、誰が今何ポイント持っているかを**Googleスプレッドシート上でそのまま**確認できます。

### 回答方法(音声・表情での採点)について

- 各問題で「録画して回答する」を押すと、スタッフの端末のカメラ・マイクが起動します。
  お客様に話しかけるつもりで声に出して回答し、「回答を終える」を押すと採点されます。
- 採点は3つの軸で行われ、それぞれ0〜100点でスコア化されます。
  1. **内容**: ブラウザ内蔵の音声認識で発話をテキスト化し、模範解答とどれくらい近い
     内容を話せていたかを判定(無料のブラウザ機能のみを使用。外部AI・有料APIは
     一切使っていません)
  2. **表情**: 笑顔の度合い・カメラへの目線をその場で解析
  3. **声のトーン**: 声の抑揚・声量をその場で解析
  - 3つを合成した総合スコアが一定点以上で「合格」となります。
- **映像・音声そのものはどこにも保存されません。** 解析はすべてスタッフの端末
  (ブラウザ)内で完結し、サーバーに送信されるのは「スコア」と「発話内容のテキスト」
  だけです。発話内容のテキストは、あとで内容を振り返られるよう「完了記録」シートに
  参考情報として記録されます。
- **対応ブラウザ: Google Chrome限定です。** 発話内容のテキスト化(音声認識)は
  Google Chrome(パソコン・Android)でのみ動作するため、このアプリはChrome以外の
  ブラウザでは利用できない仕様にしています(Chrome以外でアクセスすると、その旨を
  伝える案内画面が表示され、研修には進めません)。
- **iPhone/iPadは非対応です。** iOSでは「Chrome」という名前のアプリを入れても、
  中身はSafariと同じ仕組み(WebKit)で動いており、音声認識には対応していません。
  そのため**iPhone/iPadではこのアプリ自体が使えません**。スタッフには、Androidスマホ
  またはパソコンのGoogle Chromeでアクセスするよう案内してください。会社支給の端末が
  iPhoneの場合は、別途Android端末やPCの用意が必要になります。
- この方式は、選択式クイズと違って**採点そのものがスタッフの端末側(ブラウザ)で
  行われます**。サーバー(Apps Script)側では発話内容や表情そのものを検証できない
  ため、選択式クイズだった頃と比べると不正操作への耐性は下がります(無料の範囲で
  実現するための仕様上のトレードオフです)。

### 全体の仕組み(かんたんな説明)

- スタッフが触る「画面」と、ポイントを記録する「台帳」は別の場所にあります。
- 台帳の役割をしているのが **Googleスプレッドシート** です。ポイントの記録・確認は
  すべてこのスプレッドシートを直接見れば分かります。管理者用の特別な画面はありません。
- 画面(スタッフが実際に触るページ)は、御社の既存のホームページのフォルダに
  アップロードして使います。

```
[スタッフのスマホ] → (画面: app/dist を自社サイトにアップロード)
                          ↓ 通信
                    [Google Apps Script] → [Googleスプレッドシート(台帳)]
```

### 構成

- `app/` : スタッフ用フロントエンド(Vite + React + TypeScript)
  - `/` ログイン(店舗はプルダウンから選択、お名前(フルネーム)を入力するとその場で登録される)。
    **登録は初回のみ**。一度登録すると、その端末のブラウザに情報が保存され、次回以降は
    ログイン画面を出さずに研修メニューへ直接進む(別のスマホ・別のブラウザでは再登録が必要)
  - `app/src/config.ts` : **ここにApps ScriptのURLを1つ貼り付けるだけ**でよい設定ファイル
  - `app/src/data/stores.ts` : 店舗マスタ(店舗ID・店舗名の一覧)
    **研修問題そのものはコードの中にはなく**、Googleスプレッドシートの「問題」シートから
    毎回取得している(下記「研修問題を追加・変更したいとき」を参照)
  - `/modules` 研修メニュー(保有ポイント表示、研修への入口は1つだけ)
  - `/training` 研修本編(全問題からランダムに5問出題。カメラ・マイクを使った
    音声・表情での回答→その場で採点。5問すべて合格で1pt)
  - `app/src/scoring.ts` : 内容・表情・声のトーンのスコア計算と、良かった点/改善点の
    文章生成ロジック
  - `app/src/media/` : 音声認識(`speechToText.ts`)・表情解析(`faceAnalyzer.ts`、
    MediaPipeをCDNから動的読み込み)・声のトーン解析(`voiceAnalyzer.ts`、Web Audio API)
  - `/admin/login`, `/admin` 管理ダッシュボード(全店舗のポイント一覧・CSV出力。
    スプレッドシートを直接見れば十分な場合は使わなくてもよい)
- `apps-script/Code.gs` : **バックエンド本体**。Googleスプレッドシートの
  「拡張機能 → Apps Script」に貼り付けて使う1ファイルのプログラム。
  スタッフ登録・研修問題の配信・採点とポイント付与(1日1人1pt上限)・
  管理者向けのポイント一覧取得や手動調整を行う

### なぜこの構成か(設計メモ)

- 当初はFirebase(Google製の別クラウドサービス)を使う構成も検討しましたが、
  「専務や店舗責任者が使い慣れたスプレッドシートでそのままポイントを確認したい」
  という要望に合わせて、Googleスプレッドシート + Apps Script(スプレッドシートに
  紐づく無料のプログラム実行環境)による構成に変更しました。専用の管理画面を
  覚えなくても、スプレッドシートを開くだけで済みます。
- ポイントの加算処理自体は必ず `apps-script/Code.gs` 側で行い、スプレッドシートの
  中身をアプリから直接書き換えることはできません。ただし、各問題の合否判定(採点)は
  音声・表情解析の都合上スタッフの端末(ブラウザ)側で行われ、その結果をサーバーが
  信頼する方式になっています(上記「回答方法について」参照)。選択式クイズだった
  頃のような、サーバー側での厳密な正誤検証はできません。
- 研修は年代別モジュールではなく、**全問題からランダムに5問**を出題する単一の
  形式にしています。5問すべて正解して初めて1pt獲得です。ポイントは1日1人1ptが
  上限で、挑戦するたびに「完了記録」シートに記録は残しますが、その日にすでに
  1pt獲得済みの場合はポイントを加算しません(何回挑戦しても、同じ日のうちは
  2pt以上になりません)。同時アクセスによる書き込み競合を避けるため
  `LockService`(処理の順番待ち)も使っています。
- スタッフのログインは店舗選択+フルネーム入力のみの簡易方式です(社員番号のような
  IDは求めない設計)。同姓同名のスタッフが同じ店舗にいる場合、現状は同一人物として
  扱われます。表記ゆれ(ニックネーム表記など)がある場合もポイントが別々に記録される
  ため、スタッフには毎回同じ表記のフルネームを入力してもらう運用にしてください。
- 研修問題は「傾聴」「雑談のFORDの法則(Family/Occupation/Recreation/Dreams)」
  「クッション言葉」「高齢者とのコミュニケーションの基本」など、一般的に知られている
  接客・コミュニケーション手法を基にしたサンプル/たたき台です。実際の接客方針に
  合わせて店舗側・専務側で文言を調整してください。
- 研修問題の中身は**コードではなくGoogleスプレッドシートの「問題」シートで管理**します。
  当初はコード(TypeScriptファイル)に直接書く方式でしたが、「コードを一切触らずに
  問題を追加できるようにしたい」という要望に合わせて、スプレッドシートの行を
  追加するだけで済む方式に変更しました。採点もこのシートを見て `apps-script/Code.gs`
  側で行うため、シートを更新すればすぐに反映され、**再デプロイやビルドは不要**です。
- 問題の正解は、押しつけがましい引き止めではなく気配り・情報提供による自然な
  会話延長を選ぶ内容にしています。無理な引き止めや、困っているお客様に付け込む
  ような文言にならないよう、コンテンツを追加・変更する際もこの方針を維持してください。

### セットアップ手順(はじめての方向け)

#### 1. 台帳(Googleスプレッドシート + Apps Script)を用意する

1. [Googleスプレッドシート](https://sheets.google.com/) を開き、空白のスプレッドシートを新規作成する
2. 分かりやすいように、スプレッドシートの名前を「接客研修 台帳」などに変更する(任意)
3. 上部メニューの「**拡張機能**」→「**Apps Script**」をクリックする(別タブでエディタが開く)
4. エディタに最初から入っているコード(`function myFunction() {...}` など)を全部選択して削除する
5. このリポジトリの `apps-script/Code.gs` の中身を全部コピーして、エディタに貼り付ける
6. 貼り付けたコードの上の方にある
   ```
   var ADMIN_KEY = 'CHANGE_ME_TO_YOUR_OWN_SECRET';
   ```
   の `CHANGE_ME_TO_YOUR_OWN_SECRET` の部分を、好きな文字列(合言葉)に書き換える
   (これが管理者用のパスワードになります。他人に推測されにくいものにしてください)
7. キーボードで保存する(Windowsは `Ctrl+S`、Macは `Cmd+S`)
8. 右上の「**デプロイ**」ボタン→「**新しいデプロイ**」をクリックする
9. 「種類の選択」の歯車アイコンをクリックし、「**ウェブアプリ**」を選ぶ
10. 「次のユーザーとして実行」は **自分** のまま、「アクセスできるユーザー」は **全員** にする
11. 「**デプロイ**」ボタンをクリックする
12. 初回は「このアプリは確認されていません」という警告画面が出ることがあります。
    これは自分で作ったプログラムだから出る一般的な確認です。「詳細」→
    「(プロジェクト名)に移動」→「許可」と進んで問題ありません
13. 発行された「**ウェブアプリのURL**」(`https://script.google.com/macros/s/.../exec` の形)
    をコピーする(あとで使います)

#### 2. 画面(フロントエンド)を用意する

1. `app/src/config.ts` を開き、
   ```ts
   export const API_URL = 'PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE'
   ```
   の部分を、手順1-13でコピーしたURLに書き換えて保存する
2. 依存パッケージのインストール
   ```
   cd app && npm install
   ```
3. ビルド(画面一式のファイルを作る)
   ```
   npm run build
   ```
   実行すると `app/dist/index.html` という**1つのファイル**ができる
   (JS・CSSもすべてこの1ファイルに埋め込まれるので、「コードを貼り付ける欄」しか
   ない簡易なホームページ編集画面にもそのまま貼り付けられる)
4. ホームページの管理方法に応じて、どちらかの方法でアップロードする
   - **FTP・ファイルマネージャーが使える場合**: 公開フォルダの中に作った任意の
     フォルダ(例: `public_html/training/`)に `app/dist/index.html` をそのまま
     アップロードする
   - **「コードを貼り付ける欄」しかない場合**: `app/dist/index.html` の中身を
     テキストエディタで開いて全選択・コピーし、そのままその欄に貼り付けて保存する
5. `https://自社ドメイン/(設置した場所)/` にアクセスして、ログイン画面が表示されれば成功

### 動作確認・使い方

- **スタッフ用画面**: `https://自社ドメイン/training/`
- **管理ダッシュボード(任意)**: `https://自社ドメイン/training/#/admin/login`
  (手順1-6で決めた合言葉でログイン)
- **ポイントの確認**: 手順1で作ったGoogleスプレッドシートを開き、「スタッフ」という
  シートを見れば、店舗・氏名・ポイントの一覧がそのまま確認できます
  (研修が1つも終わっていなくても、ログインした時点で0ptの行が追加されます)
- 研修が完了すると「完了記録」シートに、景品交換などでポイントを手動で減らすと
  「ポイント調整履歴」シートに、それぞれ自動で記録が追加されます

### 研修問題を追加・変更したいとき(コード不要)

Googleスプレッドシートを開き、「**問題**」というシートに直接入力するだけです。
コードの編集・再ビルド・再デプロイ・ホームページへの再アップロードは一切不要です。

| 列 | 内容 |
|---|---|
| ID | 空欄でよい(自動で振られます) |
| 年代 | `20代` / `30〜50代` / `60〜70代` のいずれかを入力(出題は全年代から共通でランダムに行われるため、分類・整理用の情報です) |
| 場面 | 場面の説明文 |
| お客様のセリフ | お客様のセリフ |
| 選択肢1〜3 | (現在はボタンとして表示はしませんが、採点の「模範解答」として使われます。1つを正解として設定してください) |
| 正解1〜3 | 正解(模範解答)の列にだけ `○` を入力(1つだけ) |
| 解説1〜3 | 採点後の「良かった点・改善点」の文章に使われる解説文 |

※ 選択式クイズだった頃の名残の列名ですが、音声・表情での採点になった現在も
「正解の選択肢の文言・解説」は**模範解答**として使われています(スタッフの発話が
これにどれだけ近い内容だったかを判定します)。そのため、正解の文言はスタッフに
実際に声に出して言ってほしい一言をそのまま書くようにしてください。

新しい行を1行追加して保存すれば、次にスタッフがアプリを開いたときから
すぐにその問題が出題されます。既存の行を編集すれば、その場で内容が更新されます。

### 今後の拡張案

- スタッフごとのPINコード・簡易認証の強化(なりすまし防止をより厳密にしたい場合)
- 店舗別・月別のランキング表示
- 同姓同名スタッフの統合機能
